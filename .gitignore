# This repo ships the *architecture*, not any project's live state.
# Per-project artifacts the system generates at runtime never belong here:
TEAM-BOARD.md
TEAM-BOARD-INDEX.md
TEAM-BOARD-ARCHIVE-*.md

# Recorded knowledge is personal to whoever runs the system — the index
# ships as an empty scaffold and shards are created lazily on first RECORD.
knowledge/KNOWLEDGE-[0-9][0-9][0-9].md

.DS_Store
Thumbs.db

# Runtime measurement, personal to whoever runs the system.
knowledge/CONSULT-LOG.tsv
